//BRONNZE
let name1 = 'Stephanie';

let name2 = 'Austin';


console.log("length of Name:", name1.length);
console.log("length of Name:", name2.length);

//SILVER

if (name1.length > name2.length) {
    console.log(`${name1} is longer than ${name2} by ${name1.length - name2.length}`);
} else {
    console .log(`${name1} is shorter than ${name2}`)
} 

//GOLD

if (name1.length > name2.length) {
} else {
    console.log(`${name2}'s name is longer than mine by ${name2.length - name1.length}`)
}